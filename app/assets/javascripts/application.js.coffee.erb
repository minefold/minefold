# Copyright 2011 Mutli Corp.

#= require json2
#= require jquery
#= require jquery_ujs
#= require underscore
#= require backbone

#= require jquery.cookie
#= require jquery.elastic
#= require jquery.autoGrowInput

#= require_self
#= require ./pusher
#= require_tree ./models
#= require_tree ./collections
#= require_tree ./views
#= require_tree ./routers

window.Mf = {}

# TODO Transition namespace over to lowercase
window.mf = window.Mf

window.delay = (ms, fn) -> setTimeout(fn, ms)
window.every = (ms, fn) -> setInterval(fn, ms)

jQuery.fn.disable = -> $(@).addClass('disabled').attr('disabled', 'disabled')
jQuery.fn.enable  = -> $(@).removeClass('disabled').removeAttr('disabled')

