/ %h1 Latest Shots on Minefold

%div.shots
  - for shot in @shots
    %div.shot
      %a.shot-link{href: "/shots/#{shot.id}-#{shot.slug}"}
        %img{src: shot.file.quart.url, width: 240}
        - unless shot.title.blank?
          %h3.title= shot.title
        / todo: format
        %h5.date= shot.created_at
      %h4.user
        %a{href: "/shots/#{shot.creator.slug}"}=shot.creator.username
