:css
  a.button {
    color: #08D;
    text-decoration: none;
    font-size: 18px;
    line-height: 48px;
  }
  a.button:hover {
    color: #06B;
  }

- content_for :masthead do
  %h1
    %strong Hiigh five!
    You can now play for
    %strong= pluralize current_user.hours, 'hour', 'hours'
    and
    %strong= pluralize current_user.minutes, 'minute', 'minutes'
    \.

= link_to(current_user.world, class: 'button') do
  &larr; Back to #{current_user.world.name}
