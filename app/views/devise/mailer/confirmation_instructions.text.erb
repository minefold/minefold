Thanks for creating an account on Minefold. To confirm your email address, just visit the link below:

    <%= confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %>

<%= render partial: 'shared/mail_signature' %>
