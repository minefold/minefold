Hi <%= @creator.username %>,

<%= @player.username %> would like to play in "<%= @world.name %>". You can let them play in your world by going to:

    <%= world_url(@world) %>

You can also stalk <%= @player.username %> first at

    <%= user_url(@player) %>

<%= render partial: 'shared/email_sig' %>