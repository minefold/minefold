Hi <%= @user.username %>,

We've noticed you only have *30 minutes* remaining in your Minefold account. To continue playing go pro! Just visit <%= pro_account_url %>.

<%= render partial: 'shared/email_sig' %>