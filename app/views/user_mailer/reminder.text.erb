Hi <%= @user.username %>,

We've noticed you only have *30 minutes* remaining in your Minefold account. To continue playing just pop over to <%= link_to billing_account_url, billing_account_url %>.

<%= render partial: 'shared/email_sig' %>