<% title 'Servers' %>
<% layout :servers %>

<div class="dashboard">
  <div class="page-header">
    <h1>Servers</h1>
  </div>
  <%= render(partial: 'servers/list', object: @created_servers) %>
  <%= link_to 'Create a new server', new_server_path, class: %w(btn btn-large btn-block) %>
</div>
