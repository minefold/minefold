- title player.username
- masthead do
  .avatar= image_tag player.avatar.large.url
  %h1= player.username
  .meta
    - if player.user and player.user.pro?
      = pro_link(player.user)
    %span.created-at Member since #{player.created_at.strftime("%-d %b, %Y")}

%h2 Worlds

/ - user.worlds.each do |world|
/   %p= link_to strong_world_fullname(world), user_world_path(world.creator, world)
