%header
  %h1 Dashboard

%p
  You have
  %strong= pluralize user.credits, 'credit', 'credits'

= link_to 'Create new world', new_world_path

#worlds
  %h2 Worlds

  - if @worlds.empty?
    %p There are no active worlds
  - else
    %ul
      - @worlds.each do |world|
        %li
          %strong
            = link_to world.name, "/#{world.slug}"
