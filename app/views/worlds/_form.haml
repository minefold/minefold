%fieldset.name
  %h3= form.label :name

  = error_messages_for form.object, :name
  = form.text_field :name, tabindex: 1

%fieldset.description
  %h3= form.label :desc, 'Description'

  = error_messages_for form.object, :desc
  = form.text_area :desc, tabindex: 2, rows: 10

- if form.object.new?
  %fieldset.seed
    %h3= form.label :seed, 'Map seed'
    = form.text_field :seed, tabindex: 8

%fieldset.mode
  %h3 Mode

  %div
    = form.radio_button :game_mode, 0, tabindex: 2
    = form.label :game_mode_0 do
      Survival
      .hint Search for resources, crafting, gain levels, health and hunger

  %div
    = form.radio_button :game_mode, 1, tabindex: 3
    = form.label :game_mode_1 do
      Creative
      .hint Unlimited resources, free flying and destroy blocks instantly

%fieldset.difficulty
  %h3 Difficulty

  - World::DIFFICULTIES.each_with_index do |level, i|
    %div
      = form.radio_button :difficulty_level, i, tabindex: (4+i)
      = form.label "difficulty_level_#{i}", level.to_s.titlecase

%fieldset.gameplay
  %h3 Gameplay

  %div
    = form.check_box :pvp, tabindex: 9
    = form.label :pvp, 'Player vs. Player'

  %div
    = form.check_box :spawn_monsters, tabindex: 10
    = form.label :spawn_monsters, 'Spawn monsters'

  %div
    = form.check_box :spawn_animals, tabindex: 12
    = form.label :spawn_animals, 'Spawn animals'

%fieldset.submit
  - if form.object.new?
    = form.submit 'Create world', tabindex: 12
  - else
    = form.submit 'Update settings', tabindex: 12

