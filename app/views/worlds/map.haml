- title "#{world.name} map" do
  = render partial: 'header'

- if world.mapped?
  #map
  - map_asset_path = "#{Rails.configuration.map_tiles_url}/#{world.id}"

  = javascript_include_tag '//maps.google.com/maps/api/js?sensor=false'

  :coffeescript
    $.world_map path:'#{map_asset_path}'

- else
  %p#nomap
    %strong Our gremlins are still working on this this world!
    You can make them move faster by playing for a couple of minutes. That shows them you especially want it! In the mean time can we distract you with this video?

  = youtube_vid
