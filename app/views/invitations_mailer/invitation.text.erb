Hi!

Your friend <%= @friendly_name %> wants you to join them on Minefold!

<% if @invitation.message? %>
  "<%= @invitation.message %>"
<% end %>

Minefold lets you run your own servers for games like Minecraft and Team Fortress 2. When you sign up, both of you <%= @sender_name %> will get <%= pluralize(Bonuses::ReferredFriend.mins, 'min', 'mins') %> extra server time.

To sign up, just follow this link:

  <%= invitation_url(only_path: false, invitation_token: @invitation.sender.invitation_token) %>
