:css
  .active {
    background-color: #FFC;
  }


%table
  %thead
    %tr
      %td #
      %td ID
      %td Username
      %td Email
      %td Credits
      %td World

  - @users.each_with_index do |user, i|

    %tr{class: (user.username? ? 'active' : nil)}
      %td= i
      %td= user.id
      %td= user.username
      %td= user.email
      %td
        = user.format_credits
        %strong= user.credits
      %td
        - if user.world?
          = link_to user.world.name, user.world