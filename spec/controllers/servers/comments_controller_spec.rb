require 'spec_helper'

describe Servers::PostsController do

  # test 'POST #create unauthenticated' do
  #   server = Server.make!
  #
  #   post :create, server_id: server.id, comment: { body: 'test' }
  #   assert_unauthenticated_response
  # end
  #
  # test 'POST #create' do
  #   server = Server.make!
  #   user = User.make!
  #   sign_in(user)
  #
  #   assert_difference ->{ server.comments.count }, 1 do
  #
  #     post :create, server_id: server.id, comment: { body: 'test' }
  #     assert_redirected_to server_path(server)
  #
  #   end
  # end

end
