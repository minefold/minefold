require 'spec_helper'

describe InvitesController do
  # let(:user)  { create(:user) }
  # let(:world) { create :world, creator: user }
  #
  # before do
  #   @request.env["devise.mapping"] = Devise.mappings[:user]
  #   sign_in user
  # end
  #
  # describe '#create' do
  #   it "redirects back" do
  #     request.env["HTTP_REFERER"] = '/worlds'
  #     post :create, user_id: world.creator.slug, world_id: world.slug, invite: { emails: 'dave@minefold.com, chris@minefold.com' }
  #     response.should redirect_to '/worlds'
  #   end
  # end

end
